name: Daily Job Digest (IT Manager)

on:
  schedule:
    - cron: '0 7 * * *'   # Daily 07:00 UTC = 09:00 CEST / 08:00 CET
  workflow_dispatch:          # allow manual trigger from GitHub UI

jobs:
  scrape-and-notify:
    runs-on: ubuntu-latest
    permissions:
      contents: write         # needed to commit seen_jobs.json back

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r Projects/job_agent/requirements.txt

      - name: Install Playwright browser
        run: playwright install chromium --with-deps

      - name: Debug secrets
        env:
          EMAIL_ADDRESS:   ${{ secrets.JA2_EMAIL_ADDRESS }}
          EMAIL_PASSWORD:  ${{ secrets.JA1_EMAIL_PASSWORD }}
          SMTP_HOST:       ${{ secrets.JA2_SMTP_HOST }}
          SMTP_PORT:       ${{ secrets.JA2_SMTP_PORT }}
        run: |
          echo "EMAIL_ADDRESS length: ${#EMAIL_ADDRESS}"
          echo "EMAIL_PASSWORD length: ${#EMAIL_PASSWORD}"
          echo "SMTP_HOST: $SMTP_HOST"
          echo "SMTP_PORT: $SMTP_PORT"

      - name: Run job pipeline
        working-directory: Projects/job_agent
        env:
          OPENAI_API_KEY:  ${{ secrets.OPENAI_API_KEY }}
          EMAIL_ADDRESS:   ${{ secrets.JA2_EMAIL_ADDRESS }}
          EMAIL_PASSWORD:  ${{ secrets.JA1_EMAIL_PASSWORD }}
          EMAIL_TO:        ${{ secrets.JA1_EMAIL_TO }}
          SMTP_HOST:       ${{ secrets.JA2_SMTP_HOST }}
          SMTP_PORT:       ${{ secrets.JA2_SMTP_PORT }}
        run: python -u pipeline.py

      - name: Commit updated seen_jobs.json
        working-directory: Projects/job_agent
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add seen_jobs.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update seen_jobs.json [skip ci]"
            git pull --rebase origin main || git rebase --abort
            git push origin HEAD:main
          fi
